name: CI pull request

on:
  pull_request:
    branches:
      - master

jobs:
  build:
    uses: iganets/.github/.github/workflows/cmake-multi-platform.yml@mmoelle1-patch-1
    with:
      cmake_flags: "-DIGANET_OPTIONAL=examples"
    secrets: inherit
